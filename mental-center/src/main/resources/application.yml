eureka:
  client:
    service-url:
      defaultZone: http://localhost:8881/eureka/
    register-with-eureka: false
    fetch-registry: false
server:
  port: 8881

spring:
  application:
    name: center-server
